---
"wayfinder-cli": minor
---

Bare `wayfinder` now prints the wayfinder skill. It is the same bytes as `wayfinder skill wayfinder`, so an agent's first call needs no argument. The quickstart page it used to print is gone — that page only told the agent to call again. `wayfinder --help` still prints the developer's setup page.
