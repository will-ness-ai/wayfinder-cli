{
	"name": "wayfinder-cli-sandcastle",
	"private": true,
	"type": "module",
	"description": "Orchestrator dependencies for the Sandcastle lane runner. Kept out of the repo root so the root package.json stays the CLI's own.",
	"devDependencies": {
		"@ai-hero/sandcastle": "^0.12.0",
		"tsx": "^4.23.0"
	}
}
